# URL backend API (kosongkan untuk development lokal, Vite proxy yang handle)
# Untuk production, isi dengan URL backend yang sudah di-deploy
# Contoh: VITE_API_URL=https://your-backend.railway.app
VITE_API_URL=
